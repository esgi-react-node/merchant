version: "3.4"

services:
  client:
    build: .
    volumes:
      - ".:/home/node/app"
    working_dir: "/home/node/app"
    command: "yarn start"
    tty: true
    stdin_open: true
    ports:
      - 3001:3000